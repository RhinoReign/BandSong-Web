SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Optional server-side fallback only if a service role key is not available.
# Do not expose service role keys to frontend code and do not prefix them with VITE_.
SUPABASE_ANON_KEY=
